# Camera source for cv2.VideoCapture(). 0 = default built-in webcam.
# In later phases this becomes a capture-card index or an RTSP URL - no other
# code changes required.
CAMERA_SOURCE=0

# InsightFace model pack + detector input size.
INSIGHTFACE_MODEL_PACK=buffalo_l
INSIGHTFACE_DET_SIZE=640

# Cosine-similarity threshold for accepting a match as "recognized".
# Starting value only - tune with scripts/evaluate_thresholds.py.
RECOGNITION_THRESHOLD=0.50

# Minimum seconds between two gate triggers for the same recognized member,
# so one visit doesn't fire the (stub) gate every frame.
GATE_COOLDOWN_SECONDS=12

# Where embeddings, local logs, and snapshots are stored.
DATA_DIR=./data

LOG_LEVEL=INFO

# Phase 1 integration (README Section 2.2): publishes a FACE_RECOGNIZED /
# FACE_UNKNOWN event here on every logged decision. Optional - if Redis
# isn't running, publish failures are logged and swallowed; Phase 0 keeps
# working standalone either way. Port 6380 matches the repo-root
# docker-compose.yml (not the Redis default 6379 - see that file for why).
REDIS_URL=redis://localhost:6380
REDIS_RECOGNITION_CHANNEL=gate:recognition-events
